Decide whether there exists an integer $n>1$ with the following properties:
\begin{itemize}
    \item $n$ is not a prime number
    \item $a^n-a$ is divisible by $n$ for all integerss $a$. 
\end{itemize}

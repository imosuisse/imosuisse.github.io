Let $n \geq 2$ be a positive integer. For an $n$-element subset $F$ of $\{1, \ldots, 2n\}$ we define $m(F)$ as the minimum of all $\gcd{(x,y)}$, where $x$ and $y$ are two different elements of $F$. Determine the maximum value of $m(F)$.

\en {
Let $\N$ be the set of positive integers. Let $f\colon \N\to \N$ be a function such that for every $n\in \N$
\[
f(n)-n < 2021 \quad \text {and} \quad \underbrace{f(f(\cdots f(f}_{f(n)}(n))\cdots)) = n.
\]
Prove that $f(n) = n$ for infinitely many $n\in \N$.
}

\de {
Sei $\N$ die Menge der natürlichen Zahlen. Sei $f\colon \N\to \N$ eine Funktion, sodass für alle $n\in \N$
\[
f(n)-n < 2021 \quad \text {und} \quad \underbrace{f(f(\cdots f(f}_{f(n)}(n))\cdots)) = n.
\]
Beweise, dass $f(n) = n$ für unendlich viele $n\in \N$.
}

\fr { Soit $\N$ l'ensemble des entiers strictement positifs. Soit $f\colon\N\to \N$ une fonction telle que pour tout $n\in \N$
\[
f(n)-n < 2021 \quad \text {et} \quad \underbrace{f(f(\cdots f(f}_{f(n)}(n))\cdots)) = n.
\]
Montrer que $f(n)=n$ pour une infinité de $n\in \N$.
}

\ita {

}
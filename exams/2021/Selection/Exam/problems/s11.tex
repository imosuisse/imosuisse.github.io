\en{Find all even functions $g\colon\R \to \R$ for which there exists a function $f\colon\R \to \R$ such that for every $x,y \in \R$
\[
g(f(x)+y)=g(x)+g(y)+yf(x+f(x)).
\]

\emph{Remark: An even function $g$ is a function with the property $g(x) = g(-x)$ for every $x \in \R$.}
}
%Do we need this remark? \colonoscopy

\de{Finde alle geraden Funktionen $g\colon\R \to \R$, für welche eine Funktion $f\colon\R \to \R$ existiert, sodass für alle $x,y\in \R$ gilt:
\[
g(f(x)+y)=g(x)+g(y)+yf(x+f(x)).
\]
\emph{Bemerkung: eine gerade Funktion ist eine Funktion, für welche  $g(x) = g(-x)$ für alle $x\in\R$ gilt.}
}

\fr{Trouver toutes les fonctions paires $g\colon\R \to \R$ pour lesquelles il existe une fonction $f\colon\R \to \R$ tel que pour tous $x,y \in \R$ 
\[
g(f(x)+y)=g(x)+g(y)+yf(x+f(x)).
\]

\emph{Remarque: Une fonction paire $g$ est une fonction pour laquelle $g(x) = g(-x)$ pour tout  $x\in\R$.}
}